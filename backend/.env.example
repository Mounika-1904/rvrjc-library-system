# Database Configuration
# Neon PostgreSQL URL (replace with your actual connection string)
DATABASE_URL=postgres://user:password@hostname.neon.tech/dbname?sslmode=require

# JWT Secret Key
JWT_SECRET=rvrjc_library_secret_key_production_ready

# Port configuration
PORT=5000
